import type React from "react";

// Minimal type declaration for the JS-first home-landing App component.
// Allows typed import in HomePage.tsx without converting the whole
// home-landing folder to TypeScript.
declare const HomeLanding: React.ComponentType;
export default HomeLanding;
