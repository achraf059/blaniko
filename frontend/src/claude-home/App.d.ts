import type React from "react";

// Minimal type declaration for the JS-first claude-home App component.
// Allows typed import in HomePage.tsx without converting the whole
// claude-home folder to TypeScript.
declare const ClaudeHome: React.ComponentType;
export default ClaudeHome;
